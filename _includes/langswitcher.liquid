{%assign current = langs[lang]%}
<!--{{data}}-->
<button class="peer border bg-sky-300 px-6" title="{{-data.messages.chooselang[lang]-}}">{{lang}}</button>
<nav class="sr-only
			peer-focus:absolute peer-hover:absolute
			hover:absolute focus:absolute
			hover:not-sr-only focus:not-sr-only
			peer-focus:not-sr-only peer-hover:not-sr-only">
	<ul class="bg-white px-2 py-px">
		{%for lang in langs%}
			<li><a href="/{{lang[0]}}/">{{lang[1].name}}<a></li>
		{%endfor%}
	</ul>
</nav>
